## 1. Catalog
- [x] 1.1 `useQueryChallengesSwr` — mock list (~6 challenges, `Challenge` shape)
- [x] 1.2 `features/challenge/ChallengeCatalog` — search + type filter + card grid
- [x] 1.3 `[locale]/challenges/page.tsx` renders ChallengeCatalog

## 2. i18n
- [ ] 2.1 i18n `challengeSystem.*` (catalog / types / difficulty / meta counts) (vi/en)

## 3. Verify
- [ ] 3.1 build (webpack) green + tsc clean + eslint clean + JSON valid
