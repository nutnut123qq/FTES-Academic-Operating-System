"use client"

import React from "react"
import { ProfilePublic } from "@/components/features/profile/ProfilePublic"

/** `/u/[username]` — public read-only profile view (§2). */
const Page = () => <ProfilePublic />

export default Page
