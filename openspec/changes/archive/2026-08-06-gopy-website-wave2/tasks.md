# Tasks

- [x] 1. `ContentAiFab`: cáo thay sparkle ở 2 nút FAB, nền sáng + ring accent, giữ aria-label.
- [x] 2. `LessonVideoBlock` + `LessonHlsPlayer`: bỏ lớp Card bọc player.
- [x] 3. `LessonReader`: nút "Bài trước" ở header + tường phí liệt kê tên gói.
- [x] 4. `PostEngagementBar`: Facebook · X · Zalo web-intent; chỉ đếm share khi popup mở được.
- [x] 5. `PaymentModal`: đếm ngược 5' + GIỮ poll sau khi hết giờ (tránh trả tiền hai lần).
- [x] 6. `CourseHoverPreview`: CTA thêm vào giỏ.
- [x] 7. `Footer`: khối pháp nhân + link pháp lý (bỏ 3 dòng placeholder chưa có dữ liệu).
- [x] 8. BE: `q_auto,f_auto` cho URL ảnh + vá `parseDeliveryUrl` ca không có version (+2 test).
- [x] 9. i18n 14 key (vi+en) + sửa slogan `tagline`.
- [x] 10. Verify: `tsc --noEmit` sạch · `npm run build` xanh · BE 20/20 test storage · JSON i18n
      không lệch key giữa vi/en.
