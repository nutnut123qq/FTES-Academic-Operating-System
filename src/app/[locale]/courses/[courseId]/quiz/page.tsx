"use client"

import React from "react"
import { CourseQuiz } from "@/components/features/course/CourseQuiz"

/** `/courses/[courseId]/quiz` — course quiz (§4/§10). */
const Page = () => <CourseQuiz />

export default Page
