## 1. Hub
- [x] 1.1 `useQueryIntegrationsSwr` — mock list (~7 integrations, `{ id, key, category, connected }`)
- [x] 1.2 `features/integration/IntegrationHub` — category-grouped card grid (icon badge, name, category chip, status, mock action)
- [x] 1.3 `[locale]/integrations/page.tsx` renders IntegrationHub

## 2. i18n
- [x] 2.1 `integration.*` keys (title/subtitle, connected/notConnected, connect/disconnect, categories.*, services.<key>) — en/vi

## 3. Verify
- [x] 3.1 eslint clean; tokens only; dark-mode + a11y; Phosphor `*Icon`
