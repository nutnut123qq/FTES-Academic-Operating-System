import React from "react"
import { NotificationsSection } from "@/components/features/profile/Settings/NotificationsSection"

/**
 * `/profile/settings/notifications` — notification preferences as a settings section
 * (the same surface the notification centre's gear opens, on the same SWR cache).
 */
const Page = () => <NotificationsSection />

export default Page
