## Why
§7 group members. Fill the members tab with a role-filtered list.

## What Changes
- Replace /groups/[groupId]/members placeholder with a role filter + member list.
- Mock `useQueryGroupMembersSwr`; i18n `groupsHub.roles.*` (vi/en).

## Capabilities
### New Capabilities
- `group-members`: role-filtered member list.
### Modified Capabilities
- (none)

## Impact
FE only. No BE. Build stays green.
