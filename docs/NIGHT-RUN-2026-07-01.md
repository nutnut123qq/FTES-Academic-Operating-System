# Night-run log — 2026-07-01

Phiên tự trị qua đêm. Batch: Phase 0 + bắt đầu §3 (xem `NIGHT-RUN-QUEUE.md`).

## Kết luận: DỪNG loop sau iteration 1 (có chủ đích, không phải lỗi)

Loop tự dừng vì phát hiện một mâu thuẫn nền tảng giữa "build tự động qua đêm" và
"quy trình bắt buộc" ta vừa chốt. Chi tiết + khuyến nghị bên dưới. Không có build đỏ,
không mất mát; chỉ dừng lại để làm cùng bạn cho đúng.

## Đã làm (iteration 1)
- **phase0-auth-complete** — chạy trọn vòng OpenSpec **propose**: tạo change + 4 artifacts
  (proposal, design, 5 spec capability, tasks 0/16). ✓ Pipeline OpenSpec hoạt động end-to-end.
- Đã commit như **planning artifacts** (không phải code) để không mất.

## Vì sao DỪNG (3 phát hiện)
1. **Quy trình UX bắt buộc là tương tác.** CLAUDE.md ép: UI mới đi qua
   `starci-fe-ux-brainstorm` → **bạn chọn layout** → `starci-fe-ux-apply`. Bước "bạn chọn"
   không chạy được lúc bạn ngủ. Tự dựng UI mới sẽ **vi phạm chính enforcement** ta vừa cài.
2. **Auth (§1) phần lớn ĐÃ có + strict canon.** Skeleton giữ `AuthenticationModal` là
   step-machine Redux (`SignInState`/`SignUpState`) + singleton Formik: registration, OTP
   (cả sign-in/sign-up), remember-me, agree-to-terms **đã tồn tại**. Sửa sâu (2FA, recovery)
   trên code strict-canon một cách "mù" dễ tạo churn vi phạm luật nhà — tốn công gỡ hơn là lợi.
3. **§2 Profile đã bị xoá ở bước strip** → greenfield; queue giả định "hoàn thiện Phase 0 từ
   gần-số-không" là sai một phần (auth có sẵn, profile mới toanh).

## Khuyến nghị (làm cùng bạn, ban ngày)
- **Bắt đầu từ §3 Subject Workspace** (greenfield, giá trị cao) bằng `starci-fe-ux-brainstorm`
  → bạn chọn layout → `starci-fe-ux-apply` dựng on-canon. Đây là đúng quy trình + đúng chỗ đáng làm.
- **Auth (§1):** không churn tự động. Chỉ thêm phần thật sự thiếu (2FA, forgot/reset, captcha
  gate) theo từng OpenSpec change nhỏ, có bạn review — vì nó đụng Redux state-machine + Formik.
- Giữ nguyên hạ tầng đã set: OpenSpec + CLAUDE.md + hook enforcement + ROADMAP + queue.

## Trạng thái queue
Không tick mục nào (không có gì "xong" theo nghĩa code). `phase0-auth-complete` để ở trạng thái
**planning-only**; khi làm interactive sẽ mở lại phần apply.

## CẬP NHẬT (thầy còn thức) — gỡ blocker, tái vũ trang loop (v2)
- Re-scope đêm sang **greenfield §3 Subject Workspace** (auth hoãn — mature/strict-canon).
- **Brainstorm interactive xong**: thầy chọn **A · Sidebar rail** cho shell → ghi vào
  `NIGHT-RUN-QUEUE.md#Layouts đã chốt` + `.claude/design/subject-workspace.md` (commit 20825dc).
- Blocker "bạn chọn layout" ĐÃ GỠ cho shell → loop đêm chỉ **ux-apply** theo layout đã chốt.
- **Loop tái vũ trang.** Mỗi vòng: openspec change (subject-workspace-*) → ux-apply on-canon → build
  webpack+tsc → commit → tick. Item chưa có layout đã chốt (profile, sub-tab riêng) → dùng default
  on-canon + log, hoặc bỏ qua nếu là archetype lớn (profile).
- Guardrail giữ nguyên: build đỏ→restore+log; 2 đỏ liên tiếp→DỪNG; mock BE+log; không push/deploy.

### Tiến độ ux-apply
- **`subject-workspace-shell`** ✓ (làm interactive khi thầy hỏi "triển khai chưa"):
  route `/subjects/[subjectId]` + layout shell (A · sidebar rail), `SubjectWorkspaceShell`
  (CollapsibleSidebar + 3 cụm + 9 nav item + header), Overview = hub grid, 8 tab placeholder,
  mock `useQuerySubjectSwr`, i18n `subjects.*` vi/en. **Build webpack + tsc XANH**, 9 route sinh ra.
  Giả định/simplify: mock subject data (chưa có BE); Overview card hand-roll (chưa dùng PressableCard);
  không pathConfig builder (dùng path string). Đều đã log để review.
- **`subject-workspace-resources`** ✓ (loop): tab Resources — filter type (Button secondary/ghost) +
  list rows bordered (title·size·updated + Chip type + nút Tải) + section Collections (FolderIcon + count).
  Mock `useQuerySubjectResourcesSwr`, i18n `subjects.resources.*` vi/en. **Build webpack+tsc XANH**.
  Default-layout + giả định đã log: dense rows thay card (đúng house rule); nút Tải no-op (chưa BE); mock data.
- **`subject-workspace-practice`** ✓ (loop): tab Practice — card grid 4 module (Quiz/Flashcards/Coding/Leaderboard),
  mỗi card icon + title + count + CTA "Mở". Mock `useQuerySubjectPracticeSwr`, i18n `subjects.practice.*` vi/en.
  **Build webpack+tsc XANH**. Log: card hand-roll; icon provisional (confirmed set); CTA no-op; mock counts.
- **`subject-workspace-learning`** ✓ (loop): tab Learning — progress bar (done/total/percent tính trong hook) +
  list Section→Lesson, lesson xong = Chip "Đã học" (icon-free). Mock `useQuerySubjectLearningSwr`,
  i18n `subjects.learning.*` vi/en. **Build webpack+tsc XANH**. Log: progress bar + rows hand-roll; mock data; chưa nav vào lesson.

## LOOP DỪNG — hết item buildable (2026-07-02 ~00:3x)
Queue chỉ còn `profile-shell` = **HOÃN** (archetype 2-column cần brainstorm riêng) → không có item buildable → loop dừng đúng guardrail.
**Tổng đêm:** §3 Subject Workspace = shell + 4 tab content (Overview trong shell, Resources, Practice, Learning). 5 commit feature, tất cả build webpack+tsc XANH, mỗi thứ 1 OpenSpec change.
**Còn placeholder (chưa dựng):** community, members, statistics, ai, career (điều hướng được, chưa có nội dung).
**Việc sáng mai:** review 5 commit (`git log --oneline`); nâng cấp các "default/hand-roll" đã log (PressableCard, ProgressMeter block, icon per-type, wire BE) qua brainstorm từng tab; brainstorm `profile-shell` (§2).

- **`subject-workspace-community`** ✓ (loop v3): scope filter (For you/Following/Trending) + post rows (initials avatar + title + snippet + reactions Chip). Mock `useQuerySubjectFeedSwr`, i18n `subjects.community.*`. Build+tsc XANH. Log: mock feed, chưa post-detail/composer.

- **`subject-workspace-members`** ✓ (loop v3): role filter (Button) + member rows (initials avatar + name + role Chip). Mock `useQuerySubjectMembersSwr`, i18n `subjects.members.*`. Build+tsc XANH. Log: mock members, rows chưa link profile.

- **`subject-workspace-statistics`** ✓ (loop v3): metric cards (completion/active/resources/avg) + top students ranked list. Mock `useQuerySubjectStatsSwr`, i18n `subjects.statistics.*`. Build+tsc XANH. Log: mock số, chưa chart.

- **`subject-workspace-ai`** ✓ (loop v3): card grid AI tools (Tutor/Summary/Quiz/Flashcards/OCR) + CTA. Mock `useQuerySubjectAiToolsSwr`, i18n `subjects.aiTools.*`. Build+tsc XANH. Log: card hand-roll, icon provisional, CTA no-op, chưa AI thật.
