# Tasks

- [x] 1.1 `GradeResultCard` thêm cờ `scoreOwnedByTests` (đổi dòng điểm thành tiêu đề nhận xét + chú thích)
- [x] 1.2 `ChallengeSubmission` bật cờ khi bài nộp có kết quả test case (cả nhánh OFF_TOPIC)
- [x] 1.3 i18n `codeGrading.reviewTitle` / `codeGrading.reviewScoreNote` (vi + en)
- [x] 2.1 Test: có kết quả test case thì không hiện điểm AI, có hiện chú thích; không có thì hiện điểm như cũ
- [x] 2.2 `tsc --noEmit` sạch
