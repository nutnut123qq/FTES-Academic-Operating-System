"use client"

import React from "react"
import { CertificateVerify } from "@/components/features/course/CertificateVerify"

/** `/certificates/verify/[code]` — public certificate verification, no login. */
const Page = () => <CertificateVerify />

export default Page
