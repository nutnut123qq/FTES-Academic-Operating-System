## 1. Board
- [x] 1.1 `useQueryWorkflowSwr` — mock list (~8 items, `WorkflowItem` shape + unions)
- [x] 1.2 `features/workflow/WorkflowBoard` — 6 stage columns (label + count), item cards, horizontal scroll
- [x] 1.3 `[locale]/workflow/page.tsx` renders WorkflowBoard

## 2. Wiring
- [x] 2.1 i18n `workflow.*` (title/subtitle, stages.*, contentTypes.*, itemsCount, emptyColumn) — vi/en

## 3. Verify
- [ ] 3.1 eslint clean + JSON valid (build/tsc skipped per task scope)
