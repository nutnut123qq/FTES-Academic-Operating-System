# Tasks — honor-board-theme-accent

- [x] 1.1 `HonorBoardSection.tsx`: mọi `warning` → `accent` (gradient, metric, ring,
      chip, glow, orbs, icon); `rounded-3xl` → `rounded-2xl` cho 2 card; sửa comment.
- [x] 2.1 tsc + eslint sạch; preview verify bằng computed styles (0 class `warning`
      còn lại, metric = var(--accent) #3f51b5) — screenshot treo do WebGL hero headless.
- [x] 3.1 Journal: đính chính decision/card.md + design/home-landing.md + lesson
      "màu vinh danh theo theme accent" trong design/CONTENT.md.
