# Tasks — ai-hub-live-tools

## 1. Tính năng A — Envelope 1002 + poll dùng chung
- [x] 1.1 `client.ts`: success codes {200, 1002} (comment lý do Accepted/JobRef)
- [x] 1.2 Hook `useAiJobPolling` (SWR refreshInterval động, stop ở COMPLETED/FAILED/CANCELLED, isStale 90s)
- [x] 1.3 Unit test (2026-07-22 — runner vitest nay ĐÃ CÓ trong repo, hết BLOCKED): `client/client.test.ts` +2 test (1002 Accepted unwrap JobRef; 1001 vẫn throw RestError như cũ); `hooks/useAiJobPolling.test.tsx` (11 test): key null khi idle, fetcher→getJob, `refreshInterval` 2.5s khi PENDING/RUNNING và =0 ở COMPLETED/FAILED/CANCELLED (polling dừng đúng), parse result JSON/markdown/chưa-COMPLETED, stale flag đúng mốc 90s (fake timers), tắt khi terminal, reset khi đổi jobId, refresh→mutate. Tất cả PASS.
- [~] 1.4 **Vòng chất lượng**: unit ĐÃ XONG (1.3, 2 vòng viết→chạy→fix→chạy xanh); e2e vẫn auth-gated → backlog.

## 2. Tính năng B — Tile wiring
- [x] 2.1 `useQueryAiToolsSwr`: thêm `href` per tool; XÓA mentor khỏi catalog (mentor gỡ khỏi `TOOL_CATALOG`; tutor `href` undefined = intent-driven; category `teacher` gỡ khỏi `CATEGORY_ORDER`)
- [x] 2.2 `AiHub`: CTA điều hướng thật; tutor → logic học-tiếp (1 khóa `router.push` thẳng learn shell / nhiều → `TutorContinueModal` liệt kê / 0 → CTA "Khám phá khóa học" → `/courses`) dùng `useQueryMyCoursesSwr` sẵn có. Render guard `liveTools` bỏ tile không có action (tile chết). GHI CHÚ: dùng `Button onPress={router.push}` (pattern nhà ResumeCard) THAY vì `as={Link}` — HeroUI `Button` không nhận prop `as`/`href`.
- [x] 2.3 Unit test (2026-07-22, runner vitest nay có): `hooks/useQueryAiToolsSwr.test.tsx` (5 test — mentor/teacher vắng cả khi BE báo MENTOR_QA, mọi tool trừ tutor có href `/ai/tools/*`, tutor intent-driven href undefined, filter tool BE không expose feature, remaining map từ quota) + `AiHub/index.test.tsx` (6 test — tile chết (không href, không tutor) bị lọc không render, href tile push đúng route, tutor 3 nhánh: 1 khóa→push learn shell thẳng không modal / nhiều→modal list đúng href từng khóa / 0→modal browse CTA→`/courses`, quota chip echo). Tất cả PASS.
- [~] 2.4 e2e tay: bấm từng tile đi đúng nơi — BLOCKED: auth-gated + BE quota; 6 trang `/ai/tools/*` do task 3/4/5 dựng (chưa có trong lane này → href trỏ sẵn).
- [~] 2.5 **Vòng chất lượng**: unit ĐÃ XONG (2.3 — vòng 1 phát hiện assert sai namespace i18n picker, fix, vòng 2 xanh); e2e vẫn auth-gated (2.4) → backlog.

## 3. Tính năng C — 4 trang job tools
- [x] 3.1 Khung `AiToolShell` (SubPageHeader back→`/ai` + quota chip từ `useQueryAiToolsSwr`) + route `/ai/tools/{summary,flashcards,quiz,debug}` (route group `/ai` phẳng, không `(core)`; mỗi tool = `page.tsx` mỏng render feature component). Hook chung `useAiToolJob` (submit→giữ jobId→`useAiJobPolling`→result/quota/failed/stale/busy).
- [x] 3.2 Summary/Flashcards/Quiz: `LearningInput` chung (2 tab text | picker bài đã học từ `myLearnedLessons`, decode `fromGlobalId`→lessonId; empty→hint dùng text) + tham số (cardCount / questionCount+difficulty) → submit job (`language`=locale) → poll → render per-type: summary tldr+key_points+glossary+readMin; flashcards thẻ lật state-per-card; quiz làm tại chỗ chấm client (`isCorrectQuizOption` chuẩn hoá index/letter/text) + score. `AiToolModelNote` hiện model từ result; quota chip ở shell.
- [x] 3.3 Debug: code textarea (mono) + language Dropdown (12 ngôn ngữ) + mô tả → `submitCodeReviewJob({code,language,question})` → render `output` markdown qua `MarkdownContent` (chấp nhận `DebugResult|string` — parseJobResult trả raw khi worker trả markdown thuần).
- [x] 3.4 Error states: `AiJobFeedback` (ưu tiên quota→submitError→job FAILED→busy+stale). `isQuotaError` = RestError.status 429 hoặc errorCode `*QUOTA*` → message riêng, không poll. FAILED lấy `job.errorMessage`; stale 90s → nút refresh. Types job result: `SummaryResult/FlashcardsResult/QuizResult/DebugResult` (`tools/types.ts`).
- [x] 3.5 Unit test (2026-07-22, runner vitest nay có): `tools/toolPages.test.tsx` (11 test — submit body đúng cả 4 page qua `learningInputBody` THẬT: summary `{text,language}`, flashcards `{text,cardCount,language}`, quiz `{text,questionCount,difficulty,language}`, debug `{code,language,question}` trim; chặn run khi chưa có source; render 4 loại result từ fixture worker-schema: summary tldr/key_points/glossary/readMin/model, flashcards flip front↔back+hint, quiz chấm tại chỗ correct dạng letter+text+lock sau trả lời+score, debug markdown cả `{output,model}` lẫn bare string) + `tools/types.test.ts` (5 test `isCorrectQuizOption` index/chuỗi-số/chữ-cái/text/undefined) + `tools/AiToolResult/index.test.tsx` (6 test error states: priority quota→submitError→FAILED errorMessage→busy, stale hint retry gọi `poll.refresh` không resubmit, idle render rỗng) + `hooks/useAiToolJob.test.tsx` (8 test `isQuotaError` 429/`*QUOTA*`/loại khác + run giữ jobId/submitError/reset/isBusy) + `rest/ai/ai.test.ts` (contract POST url+body 5 submit fn, GET job/quota). Tất cả PASS.
- [~] 3.6 e2e tay trên dev: chạy đủ 4 tool với BE apitest — BLOCKED: auth-gated + BE quota. Route + submit + poll wired sẵn cho apitest; chưa runtime-run.
- [~] 3.7 **Vòng chất lượng**: unit ĐÃ XONG (3.5, 2 vòng chạy xanh); e2e vẫn auth-gated + BE quota (3.6) → backlog.

## 4. Tính năng D — CV builder + review UI
- [x] 4.1 REST module `career`: `getMyCv` (data null khi chưa có), `putMyCv`, `getCv(id)` + types `Cv*`/`CvProfileView`/`UpsertCvRequest` khớp shape jsonb BE (`CvProfileService` SECTION_TYPES: header obj, summary string, education/experience/projects/skills/awards arrays). `submitCvReviewJob` sẵn có (`/ai/career/cv-review`).
- [x] 4.2 `CvBuilderForm`: form Harvard 7 mục + repeater (education/experience/projects/skills/awards, links); list con (highlights/bullets/tech/skill items) sửa dạng multiline `linesToList`/`listToLines` (phẳng, không repeater lồng). Load `useSWR(getMyCv)` hydrate-once (empty khi null); Lưu = `putMyCv`. Validate client (`cvHeaderErrors`): bắt buộc `header.fullName` + `header.email` (email-shaped) → flag input + chặn save/review/pdf.
- [x] 4.3 `pdf.tsx`: template Harvard 1 cột (heading in hoa + border, bullet) qua `@react-pdf/renderer@4.5.1` (thêm dep). `renderCvPdfBlob` dynamic-import (code-split khỏi bundle trang) → download blob, KHÔNG endpoint BE. Font: đăng ký Roboto (VN) từ CDN, fallback Helvetica nếu fetch lỗi (xem BACKLOG).
- [x] 4.4 `CvUploadTab`: input/drop pdf/docx, `validateCvFile` client (mime+ext, ≤10MB) → file sai KHÔNG upload (spec). Luồng presigned platform (`presignPlatformFileUpload`→PUT→`completePlatformFileUpload`, sha256) → submit `{storageKey}`. Builder submit `{cvProfileId}` (save trước rồi review = nội dung khớp). `CvReviewResultPanel`: score badge màu theo mức + summary + strengths/improvements + sectionFeedback + model. `CvReviewTool` = `AiToolShell`+`ExtendedTabs`(builder|upload)+job dùng chung+`AiJobFeedback`. Route `/ai/tools/cv-review`. i18n vi/en `aiPlatform.toolPages.cvReview.*`.
- [x] 4.5 Unit test (2026-07-22, runner vitest nay có — thay verify tạm): `CvReviewTool/sections.test.ts` (9 test — `emptyCvSections` khớp shape jsonb BE, `linesToList`/`listToLines` round-trip + drop blank, `isEmailLike`, `cvHeaderErrors` bắt fullName+email-shaped từng field, `isCvHeaderValid`) + `CvReviewTool/upload.test.ts` (7 test — `validateCvFile`: png→"type" KHÔNG upload, >10MB→"size", pdf/docx MIME + `.docx` mime-rỗng fallback ext, đúng-10MB pass; `uploadCvFileToStorage`: presign→PUT đúng url/headers/file→complete với sha256 hex ĐÚNG checksum (đối chiếu node:crypto)→trả `objectKey`, PUT fail→throw+KHÔNG complete) + payload review 2 nhánh `{cvProfileId}`/`{storageKey}` pin ở `rest/ai/ai.test.ts`. Tất cả PASS. (PDF smoke node `%PDF-` từ đợt trước vẫn giá trị — không lặp trong vitest vì @react-pdf nặng.)
- [~] 4.6 e2e tay BLOCKED: auth-gated + dev BE trả presigned URL placeholder `storage.local` (PUT fail ở dev; cần S3 prod). Builder→save→export PDF→review(`cvProfileId`) chạy được khi có auth; upload→review chờ S3. Xem BACKLOG.
- [~] 4.7 **Vòng chất lượng**: unit ĐÃ XONG (4.5, 2 vòng chạy xanh — vòng 1 fix type `Uint8Array<ArrayBufferLike>` không phải BlobPart trong test); e2e vẫn auth/infra-gated (4.6). Findings (font PDF, upload placeholder dev) đã append BACKLOG-REVIEW-lane-ai.md.

## 5. Tính năng E — Planner UI
- [x] 5.1 REST `ai.ts`: `createStudyPlan` (timeout 120s, sync), `getStudyPlans`, `getStudyPlan`, `patchStudyPlanProgress`, `archiveStudyPlan` + types `StudyPlanView/StudyPlanContent/StudyPlanWeek/StudyPlanTask/StudyPlanInput/StudyPlanProgress/CreateStudyPlanRequest/StudyPlanProgressRequest` (khớp BE `StudyPlanController` `/ai/learning/study-plan[s]`; `plan` jsonb snake_case `est_hours/resource_hint`; `percentDone` BE tính). Endpoint đều `authenticated:true`.
- [x] 5.2 `PlannerForm`: goal (bắt buộc, TextArea) + deadline/hours **preset segmented** (house pattern như Flashcards, KHÔNG slider — 7..180 / 3..20) + currentLevel (Input) + known/target **topic chips** (Input Enter/`,`/blur → chip, dedupe cap 20, × gỡ) + model picker (`AiModelPicker` + `useGetAiCatalogModelsSwr`, null=default). Submit `CreateStudyPlanRequest` (`language`=locale) → parent chạy POST dài. `index.tsx`: empty list → thẳng form; nút "Tạo lộ trình" cũng mở form; generating = button `isPending`; create error → panel (quota 429 dùng `isQuotaError` → message riêng).
- [x] 5.3 `PlanTimeline`: `ProgressMeter` percentDone + `<ol>` mỗi tuần card (focus + milestone TargetIcon + checklist `Checkbox`). Check-off = `onToggleTask(taskKey, done)` → parent optimistic (`applyProgress` thuần recompute percent) → PATCH → reconcile server view + cập nhật SWR cache; lỗi → rollback plan cũ + `toast.danger`. Archive = `Modal` confirm (danger) → `archiveStudyPlan` → gỡ khỏi list cache + về list. `logic.ts` thuần: `buildTaskKey`/`planTaskKeys`/`isTaskDone`/`computePercentDone`/`applyProgress` (mirror BE `validTaskKeys`/`computePercentDone`). Route `/ai/tools/planner`. i18n vi/en `aiPlatform.toolPages.planner.*` (33 khoá, parity).
- [x] 5.4 Unit test (2026-07-22, runner vitest nay có — node-exercise cũ chuyển thành test THẬT): `PlannerTool/logic.test.ts` (13 test PASS, fixture mirror seed BE V250 2 tuần × 3 task): `buildTaskKey(2,0)="w2:0"` (khớp spec scenario), 6 taskKeys đúng thứ tự document, tuần malformed (week âm/thiếu) bị skip như BE guard, `isTaskDone`, percentDone seed=17 (1/6), stale key `w9:9`/nonsense bị bỏ, dedupe done-keys, plan rỗng→0, `applyProgress` check w2:0→33 / uncheck w1:0→0 / double-check idempotent / view gốc BẤT BIẾN (rollback-safe). Contract PATCH `{taskKey,done}` + create timeout 120s + archive DELETE pin ở `rest/ai/ai.test.ts`.
- [~] 5.5 e2e tay BLOCKED: auth-gated (permission `ai.learning.use`) + tiêu quota `STUDY_PLANNER`. Route + form + POST(120s) + list + optimistic check-off + archive wired sẵn cho apitest; seed V250 (1 plan demo, done `w1:0`, percent 17) hiện ngay khi login user thật đầu tiên. Chưa runtime-run.
- [~] 5.6 **Vòng chất lượng**: unit ĐÃ XONG (5.4, 2 vòng chạy xanh); e2e vẫn auth/quota-gated (5.5) → backlog.

## 6. Verify
- [ ] 6.1 `npm run build` (webpack) xanh + `tsc --noEmit` sạch; i18n vi/en đủ khoá; `openspec validate ai-hub-live-tools --strict` PASS

## Nghiệm thu E2E 2026-07-23 (spec e2e/ai-hub-live-tools.spec.ts, 3 pass / 5 fail)
- PASS: hub 7 tile CTA đúng; study planner end-to-end plan thật; build+tsc xanh (box 6.1 phần build).
- FAIL (contract FE↔BE): summary/flashcards/quiz FE gửi {text} nhưng BE đòi storageKey|lessonId|resourceId → 400 AI_INPUT_INVALID (pipeline BE sống khi submit {lessonId} trực tiếp); debug tool /ai/coding/review đòi submissionId, FE gửi code tự do → 400; CV builder GET/PUT /career/cv/me → 403 CV_PROFILE_FORBIDDEN với bearer hợp lệ (nghi bug principal-resolution BE — cùng họ bug career).
- BLOCKED-STORAGE: CV upload presigned PUT fail (storage dev placeholder).

## Nghiệm thu E2E 2026-07-24 RẠNG SÁNG — PASS phần chạy được, phần còn lại BLOCKED có lý do (GIỮ MỞ)
- ✅ Hub 7 tile CTA đúng + điều hướng /ai/tools/* (2.4) — PASS desktop+mobile.
- ✅ Planner end-to-end (5.5) — PASS (plan thật).
- ⚠️ summary/flashcards/quiz (3.6) UI e2e: BLOCKED-WAF-LOCAL — nguồn = picker "bài đã học" đọc
  GraphQL myLearnedLessons; WAF apitest chặn origin localhost (401 PLATFORM_UNAUTHORIZED) → picker
  không populate/enable từ FE local. BE JOB PIPELINE ĐÃ VERIFY qua curl (2026-07-24):
  POST /ai/learning/{summary,flashcards,quiz} {lessonId:seed-les-c1-s1-l2} → cả 3 job COMPLETED,
  summary trả TL;DR + glossary thật (model gpt-oss-120b). Prod (origin Vercel) không bị WAF.
- ✅ CV review by cvProfileId (4.6): PASS desktop (save PUT /career/cv/me + submit {cvProfileId} +
  panel Điểm mạnh/Cần cải thiện); mobile bỏ do độ trễ ai-service. Bug wave-3 "CV 403" ĐÃ HẾT sau
  fix auth-resolver 438d8f3 (curl PUT/GET /career/cv/me round-trip 200 trong phiên).
- ⚠️ debug tool: BLOCKED-BE — /ai/coding/review đòi submissionId, chưa có đường review code dán tự
  do (đo live vẫn 400 AI_INPUT_INVALID). test.fixme + ghi rõ; chờ BE mở endpoint free-paste.
- KẾT LUẬN: change GIỮ MỞ — hub+planner+CV PASS; 3 job-tool UI chờ chạy trên môi trường không-WAF
  (prod/preview Vercel) để chốt; debug chờ BE.
