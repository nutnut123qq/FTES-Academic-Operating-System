## Context
Default on-canon per NIGHT-RUN-QUEUE. Form (no SWR query hook).

## Goals / Non-Goals
**Goals:** kind chips + title + body + submit, build green.
**Non-Goals:** rich text, attach files, real post.

## Decisions
- Kind = toggle Chips; title input + body textarea (plain); submit disabled until filled; no-op.

## Risks / Trade-offs
- Plain inputs, no-op submit. Logged.
