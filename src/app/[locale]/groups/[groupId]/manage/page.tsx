"use client"

import React from "react"
import { GroupManagement } from "@/components/features/group/GroupManagement"

/** `/groups/[groupId]/manage` — group management (§7). */
const Page = () => <GroupManagement />

export default Page
