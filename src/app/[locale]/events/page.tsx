import React from "react"
import { EventCatalog } from "@/components/features/event/EventCatalog"

/** `/events` — the event catalog (§14). */
const Page = () => <EventCatalog />

export default Page
