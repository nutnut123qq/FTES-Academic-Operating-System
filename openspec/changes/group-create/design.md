## Context
Default on-canon per NIGHT-RUN-QUEUE. Form (no SWR query hook).

## Goals / Non-Goals
**Goals:** name + type + description + submit, build green.
**Non-Goals:** real create, cover image, privacy rules.

## Decisions
- Plain inputs + native select; submit disabled until named; no-op.

## Risks / Trade-offs
- No-op submit, mock. Logged.
