## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** requests + rules + pinned, build green, FE mock.
**Non-Goals:** real accept/reject, edit rules, invitations flow.

## Decisions
- Sections separated by border-t; join request rows resolve locally; rules numbered; pinned rows.

## Risks / Trade-offs
- Local-only actions, mock. Logged.
