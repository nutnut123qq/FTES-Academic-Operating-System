import React from "react"
import { WorkflowBoard } from "@/components/features/workflow/WorkflowBoard"

/** `/workflow` — the content-workflow board (§19). */
const Page = () => <WorkflowBoard />

export default Page
