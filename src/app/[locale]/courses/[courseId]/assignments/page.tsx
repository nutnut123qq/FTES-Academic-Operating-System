"use client"

import React from "react"
import { CourseAssignments } from "@/components/features/course/CourseAssignments"

/** `/courses/[courseId]/assignments` — assignments list (§4). */
const Page = () => <CourseAssignments />

export default Page
