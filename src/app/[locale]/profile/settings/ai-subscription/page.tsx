import React from "react"
import { AiPlanSection } from "@/components/features/profile/Settings/AiPlanSection"

/**
 * `/profile/settings/ai-subscription` — the purchasable FrosTES plans and the
 * checkout that buys one. Thin route file: the rail and page frame belong to
 * `SettingsShell`, mounted by the profile layout.
 */
const Page = () => <AiPlanSection />

export default Page
