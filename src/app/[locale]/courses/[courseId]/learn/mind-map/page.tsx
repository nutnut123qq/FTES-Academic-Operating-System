"use client"

import React from "react"
import { MindMap } from "@/components/features/learn/MindMap"

/** `/courses/[courseId]/learn/mind-map` — radial course mind map (§4). */
const Page = () => <MindMap />

export default Page
