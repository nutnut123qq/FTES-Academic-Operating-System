## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** questions + selectable options + local score, build green, FE mock.
**Non-Goals:** server grading, timer, attempts, review.

## Decisions
- Options = selectable button rows (selected = accent tint; after submit, correct highlighted). Score computed client-side.

## Risks / Trade-offs
- Local-only scoring, mock questions. Logged.
