import React from "react"
import { ResourceUpload } from "@/components/features/resource/ResourceUpload"

/** `/resources/upload` — upload a resource (§5). */
const Page = () => <ResourceUpload />

export default Page
