"use client"

import React from "react"
import { CommunitySaved } from "@/components/features/community/CommunitySaved"

/** `/community/saved` — the caller's bookmarked posts (BE `GET /community/bookmarks/posts`). */
const Page = () => <CommunitySaved />

export default Page
