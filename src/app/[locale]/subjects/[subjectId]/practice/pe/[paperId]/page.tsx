import React from "react"
import { SubjectPePaper } from "@/components/features/subject/SubjectPePaper"

/**
 * `/subjects/[subjectId]/practice/pe/[paperId]` — one PE (Practical Exam) paper: read
 * the exam sheet, upload an answer, read the AI grade.
 */
const Page = () => <SubjectPePaper />

export default Page
