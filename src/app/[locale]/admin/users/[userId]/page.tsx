import React from "react"
import { UserDetail } from "@/components/features/admin/UserDetail"

/** `/admin/users/[userId]` — user detail + management actions (§22 Admin CMS). */
const Page = () => <UserDetail />

export default Page
