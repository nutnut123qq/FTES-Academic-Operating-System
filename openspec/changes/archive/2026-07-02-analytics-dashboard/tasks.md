## 1. Dashboard
- [x] 1.1 `useQueryAnalyticsSwr` — mock `metrics` (~6) + `sections` (~5), SWR-shaped
- [x] 1.2 `features/analytics/AnalyticsDashboard` — metric cards grid + category tiles
- [x] 1.3 `[locale]/analytics/page.tsx` renders AnalyticsDashboard

## 2. Wiring
- [x] 2.1 i18n `analytics.*` (title/subtitle, metrics.*, sections.*, chartSoon, deltaLabel) (vi/en)

## 3. Verify
- [ ] 3.1 eslint clean + JSON valid (build/tsc not run per task constraints)
