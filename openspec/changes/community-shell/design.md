## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** header + scope tabs + nested scope pages, build green.
**Non-Goals:** the feed content itself (own change), composer.

## Decisions
- Tab nav = Buttons routing to nested scope pages; placeholders keep tabs navigable.

## Risks / Trade-offs
- Scopes are placeholders (filled by later changes). Logged.
