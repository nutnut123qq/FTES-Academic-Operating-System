"use client"

import React from "react"
import { GroupAnnouncement } from "@/components/features/group/GroupAnnouncement"

/** `/groups/[groupId]/announcements` — group announcements (§7). */
const Page = () => <GroupAnnouncement />

export default Page
