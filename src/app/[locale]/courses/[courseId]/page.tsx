"use client"

import React from "react"
import { CourseDetail } from "@/components/features/course/CourseDetail"

/** `/courses/[courseId]` — course detail / sales page (§4). */
const Page = () => <CourseDetail />

export default Page
