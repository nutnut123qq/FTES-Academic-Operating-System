import React from "react"
import { ActivityTimeline } from "@/components/features/activity/ActivityTimeline"

/** `/activity` — the user activity timeline (§18, FE surface of the Activity Engine). */
const Page = () => <ActivityTimeline />

export default Page
