## Context
Default on-canon per NIGHT-RUN-QUEUE. Deviation: placed at /u/[username] instead of /profile/[username] to avoid inheriting the owner-scoped profile shell (whose section tabs point at the viewers own pages).

## Goals / Non-Goals
**Goals:** identity header + about + skills, build green, FE mock.
**Non-Goals:** follow action, full sections, privacy controls.

## Decisions
- Standalone centered page; identity card + about + skill chips + follower count.

## Risks / Trade-offs
- Route path deviates from queue (/u/ not /profile/) by design. Mock data. Logged.
