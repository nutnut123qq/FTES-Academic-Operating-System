import React from "react"
import { CourseCatalog } from "@/components/features/course/CourseCatalog"

/** `/courses` — the course catalog (§4). */
const Page = () => <CourseCatalog />

export default Page
