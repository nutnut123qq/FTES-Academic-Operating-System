# Tasks

- [x] 1. i18n `gamification.milestones` = 6 `code` của BE (vi + en).
- [x] 2. `LeaderboardGuideShell` dùng `STREAK_7/30/100`.
- [x] 3. `fallbackName` từ BE + dùng ở `LeaderboardShell` khi thiếu bản dịch.
- [x] 4. Verify: JSON parse sạch cả 2 file · `tsc --noEmit` exit 0.
