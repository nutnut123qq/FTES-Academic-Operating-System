import React from "react"
import { LeaderboardShell } from "@/components/features/gamification/LeaderboardShell"

/** `/leaderboard` — the gamification leaderboard + progression surface (§11). */
const Page = () => <LeaderboardShell />

export default Page
