"use client"

import React from "react"
import { CourseProgress } from "@/components/features/course/CourseProgress"

/** `/courses/[courseId]/progress` — course progress + certificate (§4). */
const Page = () => <CourseProgress />

export default Page
