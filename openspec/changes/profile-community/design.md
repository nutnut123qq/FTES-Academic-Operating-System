## Context
Default on-canon per NIGHT-RUN-QUEUE. Namespaced i18n communityBoard.* to avoid clashing with sections.community.

## Goals / Non-Goals
**Goals:** count tiles + timeline rows, build green, FE mock.
**Non-Goals:** follow actions, real feed, pagination.

## Decisions
- Counts = metric tiles; activity = bordered rows (text + time).

## Risks / Trade-offs
- Mock data. Logged.
