## 1. Dashboard
- [x] 1.1 `useQueryLeaderboardSwr` — mock `me` + `board` (~8) + `badges` (~5), SWR-shaped
- [x] 1.2 `features/gamification/LeaderboardShell` — stat cards + leaderboard list + badges row
- [x] 1.3 `[locale]/leaderboard/page.tsx` renders LeaderboardShell

## 2. Wiring
- [x] 2.1 i18n `gamification.*` (vi/en)

## 3. Verify
- [ ] 3.1 eslint clean + JSON valid (build/tsc skipped per task constraints)
