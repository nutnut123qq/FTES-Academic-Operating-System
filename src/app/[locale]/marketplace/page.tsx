import React from "react"
import { MarketplaceCatalog } from "@/components/features/marketplace/MarketplaceCatalog"

/** `/marketplace` — the product catalog (§13). */
const Page = () => <MarketplaceCatalog />

export default Page
