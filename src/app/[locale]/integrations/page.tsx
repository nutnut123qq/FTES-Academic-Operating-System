import React from "react"
import { IntegrationHub } from "@/components/features/integration/IntegrationHub"

/** `/integrations` — the Integration Hub (§23). */
const Page = () => <IntegrationHub />

export default Page
