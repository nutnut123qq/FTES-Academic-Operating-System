# Design
- Dùng CoverImage (block nhà: 16:9 framed, lazy, object-cover) trong container w-28/sm:w-44 shrink-0 → thumbnail nhỏ bên phải row.
- alt="" (decorative — title carries link). Null-guard thumbnailUrl → row không ảnh vẫn gọn.
- Spacing scale gap-4 (hợp lệ 0·2·3·4·6).
