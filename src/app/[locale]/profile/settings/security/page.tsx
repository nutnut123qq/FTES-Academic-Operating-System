import React from "react"
import { SecuritySection } from "@/components/features/profile/Settings/SecuritySection"

/**
 * `/profile/settings/security` — password, two-factor authentication, signed-in
 * devices and recent sign-in activity.
 */
const Page = () => <SecuritySection />

export default Page
