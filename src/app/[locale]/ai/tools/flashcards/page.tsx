import React from "react"
import { FlashcardsTool } from "@/components/features/ai-platform/tools/FlashcardsTool"

/** `/ai/tools/flashcards` — the Flashcards job tool. */
const Page = () => <FlashcardsTool />

export default Page
