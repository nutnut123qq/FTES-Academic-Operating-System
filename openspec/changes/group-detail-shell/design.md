## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** header + tab nav + nested tab pages, build green.
**Non-Goals:** tab contents (own changes), join.

## Decisions
- Server layout resolves groupId -> client shell. Tab Buttons route to nested pages; placeholders keep them navigable.

## Risks / Trade-offs
- Tabs are placeholders (filled later). Logged.
