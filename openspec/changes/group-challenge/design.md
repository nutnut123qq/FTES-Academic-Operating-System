## Context
Default on-canon per NIGHT-RUN-QUEUE. Nested under group shell (not a tab).

## Goals / Non-Goals
**Goals:** challenge rows + join, build green, FE mock.
**Non-Goals:** submission, leaderboard.

## Decisions
- Challenge rows (title + deadline + participants Chip + join Button).

## Risks / Trade-offs
- Mock, join no-op; shows under group shell tabs (none active). Logged.
