import React from "react"
import { EventDetail } from "@/components/features/event/EventDetail"

/** `/events/[slug]` — chi tiết một sự kiện (§14); đích của mọi card trong `EventCatalog`. */
const Page = () => <EventDetail />

export default Page
