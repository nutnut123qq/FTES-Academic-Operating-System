import React from "react"
import { MembershipSection } from "@/components/features/profile/Settings/MembershipSection"

/**
 * `/profile/settings/membership` — what the community membership includes and the
 * checkout that buys it. Thin route file: the rail and page frame belong to
 * `SettingsShell`, mounted by the profile layout.
 */
const Page = () => <MembershipSection />

export default Page
