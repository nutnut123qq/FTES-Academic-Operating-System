import React from "react"
import { ConfigCenter } from "@/components/features/config/ConfigCenter"

/** `/admin/config/[category]` — the Config Center admin surface (§22/§24, Super Admin gated). */
const Page = () => <ConfigCenter />

export default Page
