"use client"

import React from "react"
import { ResourceDetail } from "@/components/features/resource/ResourceDetail"

/** `/resources/[resourceId]` — resource detail (§5). */
const Page = () => <ResourceDetail />

export default Page
