import React from "react"
import { SubjectCommunity } from "@/components/features/subject/SubjectCommunity"

/** `/subjects/[subjectId]/discussion` — the Thảo luận tab (subject-scoped post feed, re-homed from the legacy `community` segment per the workspace IA). */
const Page = () => <SubjectCommunity />

export default Page
