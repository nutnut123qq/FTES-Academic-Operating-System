import React from "react"
import { AnalyticsDashboard } from "@/components/features/analytics/AnalyticsDashboard"

/** `/analytics` — the analytics metrics dashboard (§20). */
const Page = () => <AnalyticsDashboard />

export default Page
