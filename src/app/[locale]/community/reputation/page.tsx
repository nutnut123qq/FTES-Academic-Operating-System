"use client"

import React from "react"
import { CommunityReputation } from "@/components/features/community/CommunityReputation"

/** `/community/reputation` — contributor reputation leaderboard (§6). */
const Page = () => <CommunityReputation />

export default Page
