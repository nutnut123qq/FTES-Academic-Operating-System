import React from "react"
import { ModerationQueue } from "@/components/features/admin/ModerationQueue"

/** `/admin/moderation` — system-wide report queue + moderation log (Moderator+). */
const Page = () => <ModerationQueue />

export default Page
