import React from "react"
import { SearchResults } from "@/components/features/search/SearchResults"

/** `/search` — global search results (§16 Search Platform). */
const Page = () => <SearchResults />

export default Page
