"use client"

import React from "react"
import { Leaderboard } from "@/components/features/learn/Leaderboard"

/** `/courses/[courseId]/learn/leaderboard` — course leaderboard (§4). */
const Page = () => <Leaderboard />

export default Page
