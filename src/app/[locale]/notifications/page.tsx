import React from "react"
import { NotificationCenter } from "@/components/features/notification/NotificationCenter"

/** `/notifications` — the notification center (§15). */
const Page = () => <NotificationCenter />

export default Page
