## 1. Catalog
- [x] 1.1 `useQueryProductsSwr` — mock list of ~6 products (id, name, category, priceCoin, description)
- [x] 1.2 `features/marketplace/MarketplaceCatalog` — search + category filter + card grid + mock buy
- [x] 1.3 `[locale]/marketplace/page.tsx` renders MarketplaceCatalog

## 2. Wiring
- [x] 2.1 i18n `marketplace.*` (catalog / categories / priceCoin / buy) — vi/en
- [ ] 2.2 (deferred) `marketplace` path builder + nav row — shared-file edit, follow-up change

## 3. Verify
- [x] 3.1 eslint clean + JSON valid (build/tsc not run per task constraint)
