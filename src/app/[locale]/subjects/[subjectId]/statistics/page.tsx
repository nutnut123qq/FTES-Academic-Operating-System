import React from "react"
import { SubjectStatistics } from "@/components/features/subject/SubjectStatistics"

/** `/subjects/[subjectId]/statistics` — the Statistics tab (§3). */
const Page = () => <SubjectStatistics />

export default Page
