## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** report rows + actions, build green, FE mock.
**Non-Goals:** real moderation, AI moderation, audit.

## Decisions
- Report rows (target + reason + reporter) + keep/remove Buttons; action dismisses row locally.

## Risks / Trade-offs
- Local-only resolve, mock. Logged.
