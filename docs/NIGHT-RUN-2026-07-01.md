# Night-run log — 2026-07-01

Phiên tự trị qua đêm. Batch: Phase 0 + bắt đầu §3 (xem `NIGHT-RUN-QUEUE.md`).

## Kết luận: DỪNG loop sau iteration 1 (có chủ đích, không phải lỗi)

Loop tự dừng vì phát hiện một mâu thuẫn nền tảng giữa "build tự động qua đêm" và
"quy trình bắt buộc" ta vừa chốt. Chi tiết + khuyến nghị bên dưới. Không có build đỏ,
không mất mát; chỉ dừng lại để làm cùng bạn cho đúng.

## Đã làm (iteration 1)
- **phase0-auth-complete** — chạy trọn vòng OpenSpec **propose**: tạo change + 4 artifacts
  (proposal, design, 5 spec capability, tasks 0/16). ✓ Pipeline OpenSpec hoạt động end-to-end.
- Đã commit như **planning artifacts** (không phải code) để không mất.

## Vì sao DỪNG (3 phát hiện)
1. **Quy trình UX bắt buộc là tương tác.** CLAUDE.md ép: UI mới đi qua
   `starci-fe-ux-brainstorm` → **bạn chọn layout** → `starci-fe-ux-apply`. Bước "bạn chọn"
   không chạy được lúc bạn ngủ. Tự dựng UI mới sẽ **vi phạm chính enforcement** ta vừa cài.
2. **Auth (§1) phần lớn ĐÃ có + strict canon.** Skeleton giữ `AuthenticationModal` là
   step-machine Redux (`SignInState`/`SignUpState`) + singleton Formik: registration, OTP
   (cả sign-in/sign-up), remember-me, agree-to-terms **đã tồn tại**. Sửa sâu (2FA, recovery)
   trên code strict-canon một cách "mù" dễ tạo churn vi phạm luật nhà — tốn công gỡ hơn là lợi.
3. **§2 Profile đã bị xoá ở bước strip** → greenfield; queue giả định "hoàn thiện Phase 0 từ
   gần-số-không" là sai một phần (auth có sẵn, profile mới toanh).

## Khuyến nghị (làm cùng bạn, ban ngày)
- **Bắt đầu từ §3 Subject Workspace** (greenfield, giá trị cao) bằng `starci-fe-ux-brainstorm`
  → bạn chọn layout → `starci-fe-ux-apply` dựng on-canon. Đây là đúng quy trình + đúng chỗ đáng làm.
- **Auth (§1):** không churn tự động. Chỉ thêm phần thật sự thiếu (2FA, forgot/reset, captcha
  gate) theo từng OpenSpec change nhỏ, có bạn review — vì nó đụng Redux state-machine + Formik.
- Giữ nguyên hạ tầng đã set: OpenSpec + CLAUDE.md + hook enforcement + ROADMAP + queue.

## Trạng thái queue
Không tick mục nào (không có gì "xong" theo nghĩa code). `phase0-auth-complete` để ở trạng thái
**planning-only**; khi làm interactive sẽ mở lại phần apply.
