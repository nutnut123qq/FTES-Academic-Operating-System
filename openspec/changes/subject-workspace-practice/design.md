## Context
Default on-canon layout (no dedicated brainstorm) per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** 4 module shells + counts + CTA, build green, FE mock.
**Non-Goals:** real quiz/flashcard/coding engines, leaderboard data, gating.

## Decisions
- Card grid (1 col mobile / 2 col sm). Icon + title + count meta + secondary CTA.
- Provisional icons (confirmed-compiling set); mock counts.

## Risks / Trade-offs
- Hand-rolled cards + no-op CTA + provisional icons — all logged for the Practice brainstorm.
