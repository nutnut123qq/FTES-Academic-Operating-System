import React from "react"
import { MyCourses } from "@/components/features/course/MyCourses"

/** `/courses/me` — the signed-in viewer's enrolled courses ("Khóa học của tôi"). */
const Page = () => <MyCourses />

export default Page
