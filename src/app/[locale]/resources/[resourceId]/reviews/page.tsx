"use client"

import React from "react"
import { ResourceRating } from "@/components/features/resource/ResourceRating"

/** `/resources/[resourceId]/reviews` — rate + review a resource (§5). */
const Page = () => <ResourceRating />

export default Page
