import React from "react"
import { ResourceCollections } from "@/components/features/resource/ResourceCollections"

/** `/resources/collections` — learning packs (§5). */
const Page = () => <ResourceCollections />

export default Page
