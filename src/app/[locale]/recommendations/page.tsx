import React from "react"
import { RecommendationFeed } from "@/components/features/recommendation/RecommendationFeed"

/** `/recommendations` — the "for you" recommendation feed (§17). */
const Page = () => <RecommendationFeed />

export default Page
