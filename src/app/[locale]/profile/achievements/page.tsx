"use client"

import React from "react"
import { ProfileAchievementsSection } from "@/components/features/profile/ProfileAchievementsSection"

/** `/profile/achievements` — toàn bộ huy hiệu: đã mở khoá / chưa + tiến trình đạt được. */
const Page = () => <ProfileAchievementsSection />

export default Page
