import React from "react"
import { ResourceRecommendation } from "@/components/features/resource/ResourceRecommendation"

/** `/resources/recommended` — recommended resources (§5/§17). */
const Page = () => <ResourceRecommendation />

export default Page
