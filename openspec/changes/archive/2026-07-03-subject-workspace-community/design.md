## Context
Default on-canon per NIGHT-RUN-QUEUE build conventions.

## Goals / Non-Goals
**Goals:** scope filter + post rows, build green, FE mock.
**Non-Goals:** post detail, composer, reactions write, real feed ranking.

## Decisions
- Scope filter = Buttons; post rows dense/bordered with initials avatar (icon-free); reactions = Chip.

## Risks / Trade-offs
- Mock feed, no interactions wired. Logged.
