## Context
Default on-canon per NIGHT-RUN-QUEUE. Fills the shell For You slot.

## Goals / Non-Goals
**Goals:** post rows + counts, build green, FE mock.
**Non-Goals:** composer, reactions write, ranking.

## Decisions
- Link rows (avatar + author + time + title + snippet + counts) to /community/[postId].

## Risks / Trade-offs
- Mock feed. Logged.
