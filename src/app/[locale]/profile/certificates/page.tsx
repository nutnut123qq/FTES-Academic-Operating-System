"use client"

import React from "react"
import { ProfileCertificatesSection } from "@/components/features/profile/ProfileCertificatesSection"

/** `/profile/certificates` — the learner's course certificates (§2). */
const Page = () => <ProfileCertificatesSection />

export default Page
