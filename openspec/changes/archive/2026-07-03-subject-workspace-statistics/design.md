## Context
Default on-canon per NIGHT-RUN-QUEUE build conventions.

## Goals / Non-Goals
**Goals:** metric cards + ranked list, build green, FE mock.
**Non-Goals:** real analytics, charts, time ranges.

## Decisions
- Metric cards = bg-default/40 tiles (label + big number); top students = ranked bordered rows.

## Risks / Trade-offs
- Mock numbers, no charts. Logged.
