# Tasks

- [x] 1.1 Kiểu `ExecutionCaseResult` thêm `hidden`/`name`; `CodeExecutionSummary` thêm `truncated`/`omitted`
- [x] 1.2 `ExecutionResultTable`: ô dữ liệu dòng ẩn thành nhãn "đã ẩn" + icon khoá; số thứ tự kèm chip Ẩn
- [x] 1.3 Cảnh báo trần cắt, tách khỏi cảnh báo dừng sớm
- [x] 1.4 i18n `codeGrading.{testHidden,testHiddenTag,testTruncated}` (vi + en)
- [x] 2.1 Test: đủ số dòng; dòng ẩn có verdict nhưng không lộ dữ liệu; dòng mẫu giữ nguyên; thiếu cờ thì không che
- [x] 2.2 `tsc --noEmit` sạch + vitest xanh
