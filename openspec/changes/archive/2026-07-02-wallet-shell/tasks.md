## 1. Wallet shell
- [x] 1.1 `useQueryWalletSwr` — mock `{ balance, transactions }` (~8 rows, signed by kind)
- [x] 1.2 `features/wallet/WalletShell` — balance hero + mock actions + history list + empty state
- [x] 1.3 `[locale]/wallet/page.tsx` renders WalletShell

## 2. i18n
- [x] 2.1 `wallet.*` (title/subtitle/balanceLabel/coin/actions.*/history/kinds.*/empty) vi + en

## 3. Verify
- [ ] 3.1 eslint clean + JSON valid (build/tsc skipped per task scope)
