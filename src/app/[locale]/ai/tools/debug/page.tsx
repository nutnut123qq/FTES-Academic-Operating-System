import React from "react"
import { DebugTool } from "@/components/features/ai-platform/tools/DebugTool"

/** `/ai/tools/debug` — the Code Debugger job tool. */
const Page = () => <DebugTool />

export default Page
