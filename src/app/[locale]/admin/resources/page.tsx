import React from "react"
import { CmsListPage } from "@/components/features/admin/CmsListPage"

/** `/admin/resources` — Resources CMS list (§22 Admin CMS · Resource Management). */
const Page = () => <CmsListPage domain="resources" />

export default Page
