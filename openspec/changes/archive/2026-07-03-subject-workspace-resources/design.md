## Context
Default on-canon layout (no dedicated brainstorm) per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** filter + list + collections, house blocks, build green, FE mock.
**Non-Goals:** real BE, upload/version/approval flows, per-resource detail.

## Decisions
- Type filter = Buttons (secondary active / ghost); type tag = Chip soft accent.
- Dense bordered rows (house rule: dense lists = rows, not cards). Collections = bordered rows + FolderIcon.

## Risks / Trade-offs
- Mock data; download is a no-op button (wire later). Logged.
