import React from "react"
import { AdminUsersPage } from "@/components/features/admin/AdminUsersPage"

/** `/admin/users` — user management list (§22 Admin CMS · User Management). */
const Page = () => <AdminUsersPage />

export default Page
