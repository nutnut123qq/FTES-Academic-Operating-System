"use client"
import React from "react"
import { AdminConsole } from "@/components/features/admin/AdminConsole"

/** `/admin` — the admin console landing (own access + platform roles overview + media tools). */
const Page = () => {
    return (
        <AdminConsole />
    )
}

export default Page
