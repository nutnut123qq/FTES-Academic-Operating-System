## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** hero + outline + enroll CTA, build green, FE mock.
**Non-Goals:** real enroll, payment, gated content.

## Decisions
- Hero (code tile + name + level/credits + CTA); outline = section title + lesson rows.

## Risks / Trade-offs
- Mock data; enroll CTA no-op. Logged.
