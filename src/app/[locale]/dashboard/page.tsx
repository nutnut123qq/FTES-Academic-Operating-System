import React from "react"
import { Dashboard } from "@/components/features/dashboard"

/** `/dashboard` — the logged-in cockpit (Overview · Explore · Courses · Community). */
const Page = () => <Dashboard />

export default Page
