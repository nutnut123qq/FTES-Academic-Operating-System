import React from "react"
import { RolesAdmin } from "@/components/features/rbac/RolesAdmin"

/** `/admin/roles` — the roles & permissions admin surface (§1 Authorization). */
const Page = () => <RolesAdmin />

export default Page
