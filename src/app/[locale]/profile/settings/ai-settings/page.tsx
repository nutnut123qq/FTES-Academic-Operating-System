import React from "react"
import { AiSettingsSection } from "@/components/features/profile/Settings/AiSettingsSection"

/**
 * `/profile/settings/ai-settings` — which FrosTES lane the viewer runs on. Thin
 * route file: the rail and page frame belong to `SettingsShell`, mounted by the
 * profile layout.
 */
const Page = () => <AiSettingsSection />

export default Page
