import React from "react"
import { ChallengeCatalog } from "@/components/features/challenge/ChallengeCatalog"

/** `/challenges` — the challenge catalog (§10). */
const Page = () => <ChallengeCatalog />

export default Page
