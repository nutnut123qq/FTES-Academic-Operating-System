import React from "react"
import { ResourceHub } from "@/components/features/resource/ResourceHub"

/** `/resources` — the global Resource Hub (§5). */
const Page = () => <ResourceHub />

export default Page
