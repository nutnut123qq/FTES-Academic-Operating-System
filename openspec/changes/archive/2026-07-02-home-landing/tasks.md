## 1. Copy
- [x] 1.1 i18n `homeLanding.*` (hero · bento · pillars · cta) — vi/en

## 2. Landing
- [x] 2.1 `features/home-landing/HomeLanding` — hero + bento + pillars + CTA
- [x] 2.2 `[locale]/page.tsx` renders HomeLanding (replaces skeleton placeholder)

## 3. Wiring
- [x] 3.1 Verify: npm run build (webpack) green + tsc clean + eslint clean + JSON valid
