import React from "react"
import { CareerCenter } from "@/components/features/career/CareerCenter"

/** `/career` — the Career Center (§21). */
const Page = () => <CareerCenter />

export default Page
