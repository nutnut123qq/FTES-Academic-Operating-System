import React from "react"
import { ChallengeView } from "@/components/features/challenge/ChallengeView"

/** `/challenges/[challengeId]` — the challenge solve view (§10). */
const Page = () => <ChallengeView />

export default Page
