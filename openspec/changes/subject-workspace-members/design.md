## Context
Default on-canon per NIGHT-RUN-QUEUE build conventions.

## Goals / Non-Goals
**Goals:** role filter + member rows, build green, FE mock.
**Non-Goals:** invite/manage roles, profiles link, presence.

## Decisions
- Role filter = Buttons; member rows dense/bordered with initials avatar (icon-free); role = Chip.

## Risks / Trade-offs
- Mock members, rows not linked to profiles yet. Logged.
