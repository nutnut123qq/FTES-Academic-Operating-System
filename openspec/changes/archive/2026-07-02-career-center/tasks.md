## 1. Data
- [x] 1.1 `useQueryCareerSwr` — mock `{ skills, roadmaps, jobs }`, SWR-shaped, `ponytail:` note

## 2. UI
- [x] 2.1 `features/career/CareerCenter` — title/subtitle + 3 sections (skill graph · roadmaps · jobs)
- [x] 2.2 `[locale]/career/page.tsx` renders `<CareerCenter />`

## 3. i18n
- [x] 3.1 `careerCenter.*` keys (vi/en) — title/subtitle/section labels, roadmapKeys, jobTypes, viewRoadmap/apply/progressLabel

## 4. Verify
- [x] 4.1 eslint clean + JSON valid (build/tsc skipped per task scope)
