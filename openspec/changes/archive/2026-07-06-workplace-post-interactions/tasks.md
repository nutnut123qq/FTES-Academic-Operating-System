# Tasks — workplace-post-interactions

## 1. Clickable engagement

- [ ] 1.1 `SubjectOverview.PostRow`: heart/comment `<span>` → `<Link>` tới
      `${base}/discussion`, hover đổi màu.

## 2. Verify

- [ ] 2.1 tsc sạch + build (webpack) xanh.
