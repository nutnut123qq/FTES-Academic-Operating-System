## 1. Catalog
- [x] 1.1 `useQueryEventsSwr` — mock list (~6 events, exports `Event` shape)
- [x] 1.2 `features/event/EventCatalog` — search + type filter + card grid + register CTA
- [x] 1.3 `[locale]/events/page.tsx` renders EventCatalog

## 2. i18n
- [x] 2.1 `eventSystem.catalog.*` + `eventSystem.types.*` + `eventSystem.attendeesCount` + `eventSystem.register` (vi/en)

## 3. Verify
- [x] 3.1 eslint clean; icons resolve; JSON valid
