"use client"

import React from "react"
import { ProfileCv } from "@/components/features/profile/ProfileCv"

/** `/profile/cv` — the CV builder / upload + AI review section (§2). */
const Page = () => <ProfileCv />

export default Page
