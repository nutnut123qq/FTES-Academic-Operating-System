## Context
Default on-canon per NIGHT-RUN-QUEUE. Mirrors the subject Resources tab, standalone.

## Goals / Non-Goals
**Goals:** search + type filter + list, build green, FE mock.
**Non-Goals:** upload, versioning, ratings (own changes).

## Decisions
- Plain flat search + type filter Buttons + dense rows (title/subject/size + type Chip + download).

## Risks / Trade-offs
- Plain input; mock data; download no-op. Logged.
