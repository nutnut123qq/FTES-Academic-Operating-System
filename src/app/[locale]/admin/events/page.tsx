import React from "react"
import { CmsListPage } from "@/components/features/admin/CmsListPage"

/** `/admin/events` — Events CMS list (§22 Admin CMS · Event Management). */
const Page = () => <CmsListPage domain="events" />

export default Page
