## Context
Default on-canon per NIGHT-RUN-QUEUE.

## Goals / Non-Goals
**Goals:** type filter + group cards, build green, FE mock.
**Non-Goals:** join, create (own change).

## Decisions
- Filter Buttons + card grid (name + type Chip + description + member count) linking to /groups/[id].

## Risks / Trade-offs
- Mock data. Logged.
