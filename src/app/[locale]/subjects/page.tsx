import React from "react"
import { SubjectCatalog } from "@/components/features/subject/SubjectCatalog"

/** `/subjects` — the subject catalog (§3). */
const Page = () => <SubjectCatalog />

export default Page
