## Context
Default on-canon per NIGHT-RUN-QUEUE. Profile archetype = 2-column shell (identity left + sections right) per design memory.

## Goals / Non-Goals
**Goals:** identity card + section tabs + nested section pages, build green, FE mock.
**Non-Goals:** edit forms, real data, settings hub.

## Decisions
- 2-column: sticky identity card (initials avatar) left, section tab row + content right.
- Section tabs = Buttons; active via usePathname; placeholders keep tabs navigable.

## Risks / Trade-offs
- Hand-rolled identity card + tabs; mock profile; sections are placeholders. Logged.
