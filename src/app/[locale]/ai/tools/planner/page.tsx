import React from "react"
import { PlannerTool } from "@/components/features/ai-platform/tools/PlannerTool"

/** `/ai/tools/planner` — the AI study planner (goal → weekly timeline → check-off → archive). */
const Page = () => <PlannerTool />

export default Page
