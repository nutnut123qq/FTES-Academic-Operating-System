import React from "react"
import { SavedLibrary } from "@/components/features/saved/SavedLibrary"

/** `/saved` — the "Đã lưu" library (saved resources / courses / posts). */
const Page = () => <SavedLibrary />

export default Page
